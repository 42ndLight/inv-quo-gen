// Runtime configuration injected before the app bundle loads.
// In production, this file can be regenerated at deploy time (e.g. via a
// container entrypoint script) to point at the correct backend without
// rebuilding the frontend.
window.config = {
  API_URL: "/api",
};
